{
  "permissions": {
    "allow": [
      "Exec(python3)",
      "Exec(command)",
      "Exec(uv add)",
      "Exec(uv run)",
      "Exec(git status)",
      "Exec(git diff)",
      "Exec(uv lock)"
    ]
  }
}